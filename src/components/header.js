// import librarires for making the component
import React from 'react';
import { Text } from 'react-native';

//make the component
const Header = () => {
  return <Text>Albums</Text>;
};


//make the component available to other parts of the app
export default Header;
